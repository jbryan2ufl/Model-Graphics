to run open main.exe

to build run "make"

the controls are as follows:
	movement using WASD, SPACE, and CTRL keys
	hold mouse down and move mouse to change viewing angle
	rotation objects using UIOJKL
	translate objects using arrow keys
	scale objects using scroll wheel

you can click on different .obj files to load them
the program searches for .obj files in the /data directory